{
  "name": "资源共享-删除资源",
  "type": "http_api",
  "description": "永久删除可靠来源中的 contentId。用户明确请求删除且刚刚查询详情核对目标成功后直接调用；对象不唯一或资源 ID 不可靠时先询问，不要求二次确认。只通过 `body.contentId` 传入资源 ID；不得传 `path_params`、完整 URL 或 hash。",
  "config": {
    "type": "POST",
    "base_url": "http://10.129.236.188:8787",
    "path": "/api/write/delete",
    "header": {
      "content-type": "application/json",
      "x-shutong49-api-key": "${env:STATIC_CONTENT_API_KEY}"
    },
    "query": {},
    "body": {},
    "timeout_seconds": 60
  }
}
