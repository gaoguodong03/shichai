# 项目整体系统提示词默认模板设计

版本：v1.0
日期：2026-07-19
适用范围：设置页 `app_settings.system_prompt` 的初始化、编辑和运行时读取。

## 1. 目标

`system_prompt` 字段初始化时直接包含一套可编辑的项目级默认模板。用户在这套正文上补充或修改项目规则，不通过按钮插入模板，也不只显示 placeholder。

本设计只确定项目整体系统提示词，不修改场景、主持人和专家提示词。

## 2. 初始化语义

- `settings/app.json` 不存在或没有 `system_prompt` 字段时，读取设置返回完整默认模板。
- 已经保存 `system_prompt` 字段时，保留用户原值；非空自定义内容和用户显式保存的空字符串都不被默认模板覆盖。
- 默认值由后端提供，前端继续展示 API 返回的真实字段值，不维护第二份默认正文。
- 不批量修改 `backend/data/users` 中的既有设置文件。

## 3. 默认模板职责

默认模板只保存所有场景、主持人和专家共同遵守的项目规则，以及所有角色共同使用工作区文件的方式。它不包含场景流程、主持人调度策略、专家专业职责、Skill 业务步骤或结构化输出协议。

## 4. 默认正文

```text
本项目面向【服务对象】，用于【项目总体目标】。

所有角色共同遵守：
- 【统一业务范围、术语或判断口径】。
- 【统一数据、安全或合规要求】。
- 【统一语言、表达或交付要求】。

工作区文件：
- 工作区是所有角色共享项目材料、中间结果和最终交付物的统一空间。
- 不知道工作区内有哪些文件或无法确认真实路径时，先调用 `list_workspace_directory`，不得猜测文件名。
- 当前任务依赖已有文件内容时，调用 `read_workspace_file` 读取真实内容，不根据文件名、历史摘要或推测代替文件内容。
- 新建文本文件时，调用 `write_workspace_file`，同时提供明确的工作区相对路径和完整正文。
- 修改已有文本文件时，先读取当前内容，再调用 `edit_workspace_file` 做定向修改；不要通过新建同名文件代替修改。
- 需要移动或重命名文件时，调用 `rename_workspace_file`。
- 需要建立目录结构时，调用 `mkdir_workspace`。
- 不得猜测工具已经成功。只有收到成功结果后，才能声称文件已经读取、创建、修改、移动或保存。
- 工具执行失败时，根据真实错误调整操作；路径不存在时先检查目录，不得连续猜测路径。
- 回复用户时使用工作区相对路径，不暴露平台内部目录或宿主机绝对路径。
- 产生需要其他角色继续处理、后续复用或作为正式交付物的内容时，必须写入工作区，不得只放在聊天正文中。
- 各角色只能在自身职责范围内使用工作区工具，不得借助工作区能力代替其他角色完成其专业任务。
```

## 5. 验收标准

- 未初始化的设置页直接显示完整默认正文。
- 用户保存后的自定义正文保持不变。
- 用户显式保存空字符串后仍保持为空。
- 默认正文只在一个后端 Prompt 源中维护。
- 项目整体系统提示词现有运行时注入链路继续读取同一个 `system_prompt` 字段。

