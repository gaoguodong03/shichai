# 下一步开发计划

本文档记录待开发项与后续规划。

---

## 一、暂缓内容

- 登录与认证（后续）：当前登录为**文本文件账密校验**（`config/auth_users.txt`），以下为后续可做项：
  - **接入数据库**：将用户与密码存储迁入数据库（如 SQLite/PostgreSQL），支持注册信息、密码加密存储与找回等。
  - **管理员与用户权限区分**：区分管理员与普通用户，不同权限可做不同能力（例如：仅管理员可配置 MCP/Skills、应用设置、用户管理；普通用户仅使用 Chat、个人会话与导出等）。

- Skill creator 以及群聊中的 skillcreator
- 领域报告 Skill + MCP
- MCP 沙箱方向
- 不同大模型给出不同的配置格式
- 本机执行：能使用浏览器（通过 mcp 实现，或者别的方法）
- （1）理顺在DHA中Chat、Group、Skills、MCP的关系:初步考虑，用Group代替Chat，但称之为Chat；将MCP作为隶属于Skills的资源（需要提供根据Skills的描述检查系统中是否已经配置了所需的MCP的功能）  
-（2）提供软硬一体化的解决方案，重点考虑如何让DHA具备操作本地文件的功能
-（3）是否考虑引入接入外部事件的功能（？）
-（4）是否考虑引入接入外部消息应用的功能（真的不急）
- 已经建立的 group可以加入新的 dha
- **两种 Group 发言模式**：
  - **auto**：由主持人/调度决定下一发言人，逻辑不变。
  - **manual**：创建 Group 时可选「手动」；每次用户发言前必须选择「下一发言人」，仅该 DHA 回答一次后结束，等待用户再次提问；已修复「该 DHA 回答后控制权回到主持人」的 bug（manual 下不再调用主持人，直接结束本轮）。
我要对项目进行修改，你先在本文文档重给我大概的修改意见，我核对修改意见后再进行修改：


---

## 二、其他待开发项

我要对项目进行修改，你先在本文文档中给我大概的修改意见，我核对修改意见后再进行修改：
- 产品逻辑问题”Chat与Group“： chat 的内容是重复的，也就是要合并左侧的 chat 和 group。合并后的内容是，左侧出现一个 Chat 的标识，点击之后中间出现的是group 列表， 右侧的默认的对话就是原本的 chat（新建的对话页是默认的 chat，相当于用户不知道做啥就是一个默认的 chat），用户可以邀请新的 dha 加入新的对话，这样就是新建了一个 group（在新的产品重不要出现这个概念了）（代码中的 group 名称啥的也要进行修改），出现在中间的 group 列表中。因为新加入了 group，所以会有默认的主持人，这里主持人不再是 dha，而是将主持人直接写死在代码流程中。
- 同时我觉得有些内容我还是想要保留的，首先是chat 的回答格式我很喜欢（skill 是什么，调用的工具mcp 或者 call-api或者一些 read-file，反正这是这个小蓝框还有调用返回的”原始输出“，点击就能看到调用返回的结果）。对于 group 的格式来说，头像，名称，描述都要保留，还有不同人的发言框也不一样。至于流式输出，现在不需要做，先使用最简单的版本吧。

### 修改意见（待核对）

#### 1. 产品结构：合并 Chat 与 Group

| 现状 | 修改后 |
|------|--------|
| 左侧有 Chat、Group 两个入口 | 左侧只保留一个 **Chat** 入口 |
| 点击 Chat → 中间是会话列表 | 点击 Chat → 中间是**多 DHA 对话列表**（原 group 列表） |
| 点击 Group → 中间是 group 列表 | （合并到 Chat，不再单独入口） |
| 右侧为当前会话 | 右侧：**默认是单人对话**（原 chat，新建时即此）；用户可邀请 DHA 加入 → 变为多 DHA 对话，出现在中间列表 |

#### 2. 概念与命名调整

- **产品侧**：不再使用「Group」一词，统一用「Chat」或「多 DHA 对话」等表述。
- **代码侧**：`group` 相关命名需逐步替换（如 `group_chat`、`group_id`、`group_history` 等），可考虑改为 `dhas_chat`。

#### 3. 主持人逻辑

- **现状**：主持人是某个 DHA，有 `is_leader` 等配置。
- **修改后**：主持人**不再是 DHA**，改为**写死在代码流程中的固定逻辑**（如调度下一发言人、生成主持词等），不再作为可配置的 DHA 实例。我想加一个功能，就是决定下一发言人后，有一个按钮可以显示给下一发言人的全部提示词（原数据格式），最好是可以修改的（在manual 控制的时候）

#### 4. 保留的 UI/UX

- **Chat 回答格式**：保留 skill 标签、工具调用小蓝框（MCP、call-api、read-file 等）、「原始输出」可点击查看调用结果。
- **多 DHA 对话格式**：保留头像、名称、描述；不同发言人的气泡样式区分。
- **流式输出**：暂不做复杂流式，先采用**最简单实现**（如整段返回后再展示）。

#### 5. 实现要点（供后续开发参考）

- 左侧导航：移除 Group 入口，仅保留 Chat。
- 中间列表：展示「单人对话」+「多 DHA 对话」，新建默认为单人。
- 右侧对话区：支持「邀请 DHA 加入」操作，加入后该会话进入多 DHA 列表。
- 主持人：从 DHA 配置中剥离，改为后端/前端的固定调度逻辑。

### 已完成（本次开发）

- 群聊 manual 模式增强：
  - 在多 DHA 群聊中，手动选择下一发言人时，可通过「查看/编辑提示词」按钮预览该 DHA 即将收到的完整提示词（由后端根据当前群聊历史生成），并在发送前进行修改。
  - 后端 `group_chat` API 支持可选 `custom_prompt` 字段，若提供则优先作为本轮首个 DHA 的提示词，保证与前端编辑内容一致。
- Chat / Group 产品结构调整：
  - 左侧导航移除独立的 Group 入口，仅保留一个 Chat 入口；Chat 模块内部通过「单人对话 / 多 DHA 对话」切换视图。
  - 中间列在 Chat 模块下支持两种列表：单人对话列表（原 Chat 会话列表）和多 DHA 对话列表（原 Group 列表），新建多 DHA 对话入口从「新建 Group」改为「新建多 DHA 对话」。
  - 右侧内容区：当视图为单人对话时沿用原 Chat 回答格式（skill 标签、小蓝框、原始输出等）；当视图为多 DHA 对话时沿用原群聊 UI（头像、名称、描述、不同人的发言框），并支持 manual/auto 两种发言模式及提示词编辑功能。

## 三、完成项目

### Chat 上下文记忆说明（核查结果）

- **存储**：`_CHAT_HISTORY[session_id]` 存该会话的完整消息列表（HumanMessage / AIMessage）；`_TURN_SUMMARIES[session_id]` 存每轮结束后的 LLM 摘要（仅保留最近 10 轮，`_HISTORY_WINDOW_TURNS = 10`）。
- **每轮传给模型的内容**：`_build_history_summary(session_id, history)` 生成「历史摘要」字符串：
  - **优先**：若存在 `_TURN_SUMMARIES`，取最近 10 轮摘要，格式为「第 N 轮：摘要文本」；总长超过 `_HISTORY_SUMMARY_MAX_TOTAL_CHARS`（3200）时从最早轮开始丢弃。
  - **回退**：无 Turn 摘要时，按原始 history 的 Human+AI 对，取最近 10 轮，每轮截断为「用户：前 200 字」「助手：前 200 字」的预览。
- **请求时**：`user_content = "历史对话摘要：\n" + history_summary + "\n\n当前用户输入：\n" + request.message`，整段作为**一条** HumanMessage 传给 Agent；即模型每轮只收到「历史摘要 + 当前输入」，不直接收到完整消息列表。
- **轮次摘要写入**：流式/非流式回复结束后，调用 `_append_turn_summary(session_id, summary)`；summary 由 LLM 对「本轮用户+助手内容」做摘要生成，单条摘要超长会截断到 `_TURN_SUMMARY_MAX_CHARS`。摘要与 history 会持久化到 `data/sessions/`（history.json、turn_summaries.json、meta.json）。

