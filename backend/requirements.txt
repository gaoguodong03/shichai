# 后端运行依赖（版本已固定，便于环境与镜像一致）

# Web
fastapi==0.128.0
uvicorn[standard]==0.40.0

# LLM message/client compatibility
langchain-core==0.1.53
langchain-openai==0.1.7

# MCP SDK（stdio / streamable HTTP 连接远程 MCP）
mcp==1.25.0

# OpenSandbox SDK（底层沙箱基础设施，强制启用；需与 docker-compose 中 OpenSandbox 组件版本配套）
opensandbox==0.1.5

# HTTP
httpx==0.28.1

# 网页正文提取（call_api 等对 HTML 响应抽正文）
trafilatura>=1.6.0

# 配置与校验
pydantic==2.12.5
pydantic-settings==2.12.0
python-dotenv==1.2.1
pyyaml==6.0.3

# 文件读取 MCP：read_pdf、read_docx、read_xlsx
pypdf>=4.0.0
python-docx>=1.0.0
openpyxl>=3.1.0

# 测试（下限对齐 pytest 8 + asyncio 插件；不设上限以便跟随安全修复）
pytest>=8.2.0,<10
pytest-asyncio>=0.24.0
