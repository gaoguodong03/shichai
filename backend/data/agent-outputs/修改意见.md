我想要设计一个 MCP 的实现，通过https://github.com/anthropics/claude-code/blob/main/plugins/plugin-dev/skills/mcp-integration/SKILL.md这个项目的官方形式，你看能为我修改一下我的skills 和 MCP 形式吗？

我现在觉得，对于每一个数字人来说，skills 和 mcp（准确来说是工具）的对应关系应该是：每一个 skills 对应一个或多个mcp 文件夹中的工具，因此在调用的过程中应该是先找到这个人的humans 表配置该大模型，读取SKILLS中的内容给大模型判断，以此为依据进入到对应的 skill 表内，通过其中的的 SKILL 字段读取 SKILL.md 来执行 mcp 中的内容、通过在该表的后面存储对应的工具信息 json
最后，对于这个人文件夹的 mcp 也应该保留一份工具总清单，但是只用负责知道这个人都有什么 tools 就行，方便对这个人的某个 tool 进行个性化的更改

并不是所有 skill 都需要 tool，设计一个没有 tool 的：口头禅 skill，每次输出都会在最开始加入“北京邮电大学天下第一”

我想在前端聊天时显示部分日志，只显示，当询问问题后时调用哪个 skill 了，然后调用那个 tool 了。后端是不是需要加入一些接口什么的，修改完毕后顺便把需要告诉前端的内容给我