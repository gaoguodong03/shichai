pendulum==3.2.0
python-pptx==1.0.2
httpx
typing_extensions
pandas
openpyxl
xlrd

# 全局 sandbox 依赖只放多数 Skill 共用的小依赖。
# 浏览器/爬虫类重依赖（playwright、patchright、scrapling 等）不要放这里，
# 否则任意 Skill 都会触发 Chromium 下载；请放到对应 Skill 的 requirements.txt 或专用沙箱镜像中。
